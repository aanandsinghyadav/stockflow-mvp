[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
cmds = ["./node_modules/.bin/prisma generate", "./node_modules/.bin/nest build"]

[start]
cmd = "./node_modules/.bin/prisma migrate deploy && node dist/main"
