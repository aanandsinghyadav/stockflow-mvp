{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build && npm prune --production"
  },
  "deploy": {
    "startCommand": "node_modules/.bin/prisma migrate deploy && npm run start:prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
