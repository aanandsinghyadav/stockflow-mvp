{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --include=dev && node node_modules/prisma/build/index.js generate && node node_modules/@nestjs/cli/bin/nest.js build"
  },
  "deploy": {
    "startCommand": "node node_modules/prisma/build/index.js migrate deploy && node dist/main",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
